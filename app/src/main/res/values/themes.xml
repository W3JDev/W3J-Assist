<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.ProjectAura" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <item name="colorPrimary">@color/aura_blue</item>
        <item name="colorPrimaryVariant">@color/aura_blue_dark</item>
        <item name="colorSecondary">@color/aura_accent</item>
    </style>

    <style name="Theme.ProjectAura" parent="Base.Theme.ProjectAura" />
    
    <!-- Overlay window style -->
    <style name="OverlayTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>
</resources>